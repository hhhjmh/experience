# 项目经历

### 教师直播项目

###### CRUD部分

```xm
<resultMap id="BaseResultMap" type="com.group3.bean.User">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Apr 20 14:18:05 CST 2018.
    -->
    <result column="Username" jdbcType="CHAR" property="username" />
    <result column="Password" jdbcType="CHAR" property="password" />
    <result column="nickname" jdbcType="CHAR" property="nickname" />
    <result column="realname" jdbcType="CHAR" property="realname" />
    <result column="gender" jdbcType="INTEGER" property="gender" />
    <result column="telephone" jdbcType="CHAR" property="telephone" />
    <result column="type" jdbcType="INTEGER" property="type" />
  </resultMap>
  <select id="selectByUsername" parameterType="String" resultType="com.group3.bean.User">
    select * from user
    where Username = #{username}
    </select>      
    <insert id="insertUser" parameterType="com.group3.bean.User">   
    insert into user (Username, Password, nickname, realname, gender, telephone,type)
    values (#{username}, #{password}, #{nickname}, #{realname}, #{gender}, #{telephone}, #{type})
    </insert>
  <update id="updateUser" parameterType="map">
    update user set nickname = #{nickname}, realname = #{realname}, gender = #{gender}, telephone = #{telephone} where Username = #{username}   
  </update>
  <update id="renamepassword" parameterType="map">
    update user set Password = #{password} where Username = #{username}   
  </update>
```

###### 上传文件部分代码

```java
@CrossOrigin(origins="*",maxAge=3600)
	 @RequestMapping(value="/upload.action")
	   public String upload(@RequestParam("cover") MultipartFile file, HttpServletRequest request) {
	       try {
	           //获取文件名
	           String realFileName = file.getOriginalFilename();
	           // 获取当前web服务器项目路径
	           String ctxPath = request.getSession().getServletContext().getRealPath("/")+ "fileupload/";
	           System.out.println(ctxPath);
	           url = "fileupload/" + realFileName; 
	           System.out.println(url);
	           // 创建文件夹
	           File dirPath = new File(ctxPath);
	           if (!dirPath.exists()) {
	               dirPath.mkdir();
	           }
	           // 创建文件
	           File uploadFile = new File(ctxPath + realFileName);
	           // Copy文件
	           FileCopyUtils.copy(file.getBytes(), uploadFile);
	           String photo = ctxPath + realFileName;
	           System.out.println(photo);
	       } catch (Exception ex) {
	           return "failure";
	       }
	       return "success";
	   }
```

###### 验证码部分代码

```java
public class SendCode {
	 //发送验证码的请求路径URL
    private static final String
            SERVER_URL="https://api.netease.im/sms/sendcode.action";
    //网易云信分配的账号，请替换你在管理后台应用下申请的Appkey
    private static final String 
            APP_KEY="efea8da649acbce7f53164097afbd9e6";
    //网易云信分配的密钥，请替换你在管理后台应用下申请的appSecret
    private static final String APP_SECRET="114189d82de8";
    //随机数
    private static final String NONCE="123456";
    //短信模板ID
    private static final String TEMPLATEID="3952716";
    //手机号
    //验证码长度，范围4～10，默认为4
    private static final String CODELEN="6";

    public String msg(String s) throws Exception {

		DefaultHttpClient httpClient = new DefaultHttpClient();
        HttpPost httpPost = new HttpPost(SERVER_URL);
        String curTime = String.valueOf((new Date()).getTime() / 1000L);
        /*
         * 参考计算CheckSum的java代码，在上述文档的参数列表中，有CheckSum的计算文档示例
         */
        String checkSum = CheckSumBuilder.getCheckSum(APP_SECRET, NONCE, curTime);

        // 设置请求的header
        httpPost.addHeader("AppKey", APP_KEY);
        httpPost.addHeader("Nonce", NONCE);
        httpPost.addHeader("CurTime", curTime);
        httpPost.addHeader("CheckSum", checkSum);
        httpPost.addHeader("Content-Type", "application/x-www-form-urlencoded;charset=utf-8");

        // 设置请求的的参数，requestBody参数
        List<NameValuePair> nvps = new ArrayList<NameValuePair>();
        /*
         * 1.如果是模板短信，请注意参数mobile是有s的，详细参数配置请参考“发送模板短信文档”
         * 2.参数格式是jsonArray的格式，例如 "['13888888888','13666666666']"
         * 3.params是根据你模板里面有几个参数，那里面的参数也是jsonArray格式
         */
        String MOBILE= s;
        
        nvps.add(new BasicNameValuePair("templateid", TEMPLATEID));
        nvps.add(new BasicNameValuePair("mobile", MOBILE));
        nvps.add(new BasicNameValuePair("codeLen", CODELEN));

        httpPost.setEntity(new UrlEncodedFormEntity(nvps, "utf-8"));

        // 执行请求
        HttpResponse response = httpClient.execute(httpPost);
        /*
         * 1.打印执行结果，打印结果一般会200、315、403、404、413、414、500
         * 2.具体的code有问题的可以参考官网的Code状态表
         */
        String massage = EntityUtils.toString(response.getEntity(), "utf-8");

        System.out.println(EntityUtils.toString(response.getEntity(), "utf-8"));
        return massage;
    }

	
}
```



### CBOSS后台项目

```tex
鉴于这个公司的保密协议我不能粘贴代码上去，所以就简述一下这个后台项目的功能，这个项目需要完成一些移动公司移交来的任务，主要对工程师的要求是写一些接口功能以实现省公司和移动公司的交互，并将文档中的属性使用bean类声明，同时还要将这些存入Oracle数据库，此外再提交代码前进行单元测试，并查看Oracle中有没有修改好相应的文件属性，然后接口负责调用省公司的数据库，将获取到的数据封装成json字符串返回给移动公司。
```



### 跨区查询前台项目

```tex
项目只是用CDN方式嵌入了VUE，并不算是一个完整的VUE项目，借助VUE对数据的简单操作，将需要的值放入DATA的相应对象中，通过method调用对应的点击事件，通过axios的get方法发送url请求和对应的对象封装好的json字符串给后台处理。
```





### 项目经历发送

###### 技术栈：

```html
Springboot bootstrap thymeleaf mail mysql
```

###### 项目具体实现

```tex
本项目是一个基于Springboot框架搭建的jar工程，前端框架为bootstrap,并用thymeleaf代替了传统的jsp来对数据进行处理，使用了mysql完成数据库的链接，并且通过spring-boot-starter-mail完成邮件系统，使得可以将整个项目中的项目经历全部发送到你的邮箱中。
```

